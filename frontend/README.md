# Frontend

React + TypeScript SPA built with Vite.

## Dependencies

- **React Router** — client-side routing
- **Zustand** — state management

## Run

```bash
npm install
npm run dev
```

## Lint

```bash
npx eslint .
```

Config is in `eslint.config.js`.
