# Backend

Go HTTP server using the standard library.

## Run

```bash
go run .
```

Listens on `:8080` by default. Set the `PORT` env var to override.

## Lint

```bash
golangci-lint run
```

Config is in `.golangci.yml`.
